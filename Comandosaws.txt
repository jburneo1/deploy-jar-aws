aws cloudformation create-stack --stack-name pruebaConsolaAws --template-body file://test-cloud-formation.json --create stack from console
create-stack --stack-name prueba --template-body file://test-cloud-formation-with-parameter.json --parameters file://parameter.json --with parameters
// instalar en la ec2 el utilitario de mysql (Dentro de la instancia)
sudo yum install mysql -y

// conectandose a la instancia privada
mysql -h "url-instance" -u root -p

// generar jar
mvn -f api-persona/pom.xml clean package -Dmaven.test.skip=true